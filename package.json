{
  "name": "@thejonnyr/head-start",
  "version": "1.0.0",
  "description": "A project setup tool for Node.js",
  "main": "src/cli.js",
  "scripts": {
    "test": "node src/cli.js init temp/",
    "init": "bash src/cli.js init temp/ --bypass",
    "build": "npm-run-all build-*",
    "build-cli": "esbuild src/cli.js --bundle --outfile=dist/cli.js --platform=node --external:./node_modules/*",
    "build-dot-folder": "cp -R src/resources/.head_start dist/",
    "prettier-src": "npx prettier --write . src/"
  },
  "bin": {
    "head-start": "dist/cli.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jtw-r/head-start.git"
  },
  "keywords": [
    "project",
    "structure"
  ],
  "author": "jtw-r",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/jtw-r/head-start/issues"
  },
  "homepage": "https://github.com/jtw-r/head-start#readme",
  "devDependencies": {
    "@types/node": "^17.0.10",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.5.1",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "esbuild": "^0.14.13",
    "fs-extra": "^10.0.0",
    "prompt-sync": "^4.2.0",
    "prompts": "^2.4.2",
    "yargs": "^17.3.1"
  }
}
