{
  "name": "temp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "something": "",
    "ts-compile": "tsc --build --force",
    "watch-ts": "nodemon --watch src/ts -e ts --exec 'npm run-script ts-compile'",
    "deployment-branch": "echo gh_pages",
    "compile": "npm-run-all *-compile",
    "build": "npm run-script compile"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.14",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.4.4"
  }
}
