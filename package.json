{
  "name": "temp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "something": "",
    "ts-compile": "tsc --build --force",
    "watch-ts": "nodemon --watch src/ts -e ts --exec 'npm run-script ts-compile'",
    "scss-compile": "node-sass --output-style expanded --source-map true --source-map-contents true --precision 5 src/scss/ -o build/css/",
    "watch-scss": "nodemon --watch src/scss -e scss --exec 'npm run-script css-compile'",
    "compile": "npm-run-all *-compile",
    "build": "npm run-script compile"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^6.0.1",
    "nodemon": "^2.0.14",
    "npm-run-all": "^4.1.5",
    "snowpack": "^3.8.8",
    "typescript": "^4.4.4"
  }
}
